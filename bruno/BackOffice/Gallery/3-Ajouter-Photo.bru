meta {
  name: 4-Ajouter-Photo-Galerie
  type: http
  seq: 1
}

patch {
  url: http://localhost:8081/galeries/{{galerie_id}}/photos
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "photo_id": "{{photo_id}}"
  }
}

